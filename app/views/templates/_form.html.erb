  <%= bootstrap_form_for @template do |f| %>

  <div>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.text_area :description %>
  </div>

  <div>
    <%= f.submit 'Save' %>
  </div>

  <% end %>
