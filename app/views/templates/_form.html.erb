<div class="row">
  <div class="col-md-3"></div>
    <div class="col-md-6">
      <%= bootstrap_form_for @template do |f| %>
        <div>
          <%= f.text_field :name, placeholder: "e.g. Monthly Student Surveys" %>
        </div>
        <div>
          <%= f.submit 'Continue' %>
        </div>
      <% end %>
    </div>  
  <div class="col-md-3"></div>
</div>